package com.github.minigdx.tiny.graphic

object PixelFormat {
    // Color assigned using R (Red) G (Green) B (Blue) component
    const val RGA = 3

    // Color assigned using R (Red) G (Green) B (Blue) A (Alpha) component
    const val RGBA = 4

    // Color assigned by an index in a color palette
    const val INDEX = 1
}
